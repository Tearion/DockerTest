apiVersion: v1
kind: Service
metadata:
  name: dockertest
  labels:
    app: dockertest
spec:
  type: LoadBalancer
  ports:
  - port: 4711
    name: webservice-port
    protocol: TCP
    targetPort: 3000
  selector:
    app: dockertest